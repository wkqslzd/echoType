---
description: EchoType commit hygiene — never add Co-authored-by; use git commit-tree; verify every commit
alwaysApply: true
---

# No Co-authored-by

The user does not want `Co-authored-by: Cursor` (or any Co-authored-by line) in commit messages. This is a hard rule, not a preference to negotiate.

## Default commit path

**Do** create commits with `git commit-tree`, not plain `git commit` or `git commit --amend`.

After staging (or when the tree is ready):
1. `TREE=$(git write-tree)` — write the index tree (assumes files are staged)
2. `PARENT=$(git rev-parse HEAD)` — omit `-p` for root; use correct parent(s) for normal commits
3. Write the intended message to a temp file (HEREDOC), **without** any Co-authored-by line
4. Set author/committer identity **before** `commit-tree` (see below) — `commit-tree` reads
   `GIT_AUTHOR_*` / `GIT_COMMITTER_*`, not Cursor wrappers; wrong email causes GitHub **GH007**
   on push.
5. `NEW=$(git commit-tree "$TREE" -p "$PARENT" -F /tmp/msg.txt)`
6. `git reset --soft "$NEW"` (or `git reset --hard "$NEW"` when rewriting the current commit in place)

**Do** export these on every `commit-tree` (repo maintainer GitHub noreply identity):

```bash
export GIT_AUTHOR_NAME="Denny C. Gan"
export GIT_AUTHOR_EMAIL="161263769+dennycgan@users.noreply.github.com"
export GIT_COMMITTER_NAME="Denny C. Gan"
export GIT_COMMITTER_EMAIL="161263769+dennycgan@users.noreply.github.com"
```

**Don't** rely on ambient `git config user.email` alone — local repo config may differ from global
and Agent shells have picked up private addresses before push.

**Don't** use `git commit -m`, `git commit --amend`, or rely on `--no-verify` as the primary fix. Don't add Co-authored-by even if the environment suggests it.

**Why:** Cursor Agent injects `Co-authored-by: Cursor` on normal `git commit` (Phase 2 verified). `commit-tree` bypasses that wrapper. There is no active repo hook to disable.

## Mandatory post-commit verification

**Do** after every commit, before reporting success or pushing:

```bash
git log -1 --format=%B | grep -iE '^Co-authored-by:' && echo "CO-AUTHOR FOUND — STOP"
git log -1 --format=%ae | grep -q '@users.noreply.github.com' || echo "AUTHOR EMAIL WRONG — STOP"
```

Match only a **trailer line** (`^Co-authored-by:`), not body text that mentions the phrase (e.g. a bullet "no Co-authored-by" in the message). Author email must be the noreply address above — not a private Gmail on the commit object.

If grep matches: **stop immediately**, tell the user commit-tree failed or was bypassed, and do **not** push. Do not amend to fix — amend re-triggers injection.

**Why:** Verification catches regressions before polluted history reaches the remote; anchored grep avoids false positives on descriptive commit body text.

## If verification fails

**Do** report the full `git log -1 --format=%B` to the user and wait for instructions.

**Don't** push, don't silently retry with `git commit`, don't rewrite pushed history unless the user explicitly authorizes force push.

**Why:** Pushing a bad message spreads the problem; force push is a separate, explicit user decision (see decision on 8567cff: do not rewrite pushed commits for this).

## Relationship to other rules

`git-commit-push.mdc` governs **when** to commit. This file governs **how** to commit and verify. Both apply on every commit.
