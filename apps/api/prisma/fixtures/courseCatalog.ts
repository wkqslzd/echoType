import { ONBOARDING_CATALOG } from '@echotype/shared';

/**
 * Phase 6 onboarding catalog — canonical data lives in @echotype/shared.
 * Re-export for API seed scripts that import this path.
 */
export { ONBOARDING_CATALOG };
